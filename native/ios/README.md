# Native iOS modules

Swift Turbo Native Modules live here in later phases (Keychain / Secure Enclave wrapping, app lock, local notifications).

Phase 1: no native modules. iOS must not depend on Android-only capabilities.
