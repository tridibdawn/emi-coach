# Native Android modules

Kotlin Turbo Native Modules live here in later phases (`SecureKeyModule`, `AppLockModule`, local notifications).

Phase 1: no native modules and **no** `NotificationListenerService`.
