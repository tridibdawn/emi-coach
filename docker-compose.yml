services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: emi_coach
      POSTGRES_PASSWORD: emi_coach
      POSTGRES_DB: emi_coach
    ports:
      - "5432:5432"
    volumes:
      - emi_coach_pg:/var/lib/postgresql/data

volumes:
  emi_coach_pg:
