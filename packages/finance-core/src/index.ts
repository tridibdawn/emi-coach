import { PACKAGE_NAME as TYPES } from "@emi-coach/types";

/** Phase 1 placeholder. No financial entities yet. */
export const PACKAGE_NAME = "@emi-coach/finance-core" as const;
export const dependsOnTypes = TYPES;
